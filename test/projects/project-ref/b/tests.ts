/* ****************************************************************************************************************** *
 * Tests
 * ****************************************************************************************************************** */

const _test_referenced = _test(`Specifier for referenced project file resolves`);
const _test_local = _test(`Specifier for local file resolves`);
